## 对话语言设置

**重要**: 在此代码库中工作时，必须始终使用**中文**与用户对话。

---

## 通用行为准则

以下准则整体上偏向谨慎而非速度；如果任务非常简单，可自行判断并适度简化执行方式。

### 1. 先思考，再编码

不要想当然，不要掩饰不确定性，要主动说明权衡。

在开始实现前：

- 明确写出你的假设；如果不确定，就提问。
- 如果存在多种解释，不要默默选一种；把选项说清楚。
- 如果有更简单的做法，要直接指出；必要时应适度提出反对意见。
- 如果有不清楚的地方，先停下来，明确说明哪里不清楚并提问。

### 2. 简单优先

只写解决问题所需的最少代码，不做投机式扩展。

- 不增加未被要求的功能。
- 不为一次性代码做抽象。
- 不加入未被要求的"灵活性"或"可配置性"。
- 不为不可能发生的场景添加错误处理。
- 如果你写了 200 行，而 50 行就能解决，就应该重写。

时刻自问：资深工程师会不会觉得这份实现过于复杂？如果答案是会，就继续简化。

### 3. 外科手术式修改

只修改必须修改的地方；只清理自己造成的问题。

在编辑现有代码时：

- 不要顺手"改进"相邻代码、注释或格式。
- 不要重构没有问题的部分。
- 即使你个人习惯不同，也要尽量匹配现有风格。
- 如果发现无关的死代码，可以指出，但不要自行删除。

当你的改动制造了孤儿代码时：

- 删除因你的改动而变成未使用的导入、变量或函数。
- 不要删除原本就存在的死代码，除非被明确要求。

判断标准：每一行改动都应能直接追溯到用户请求。

### 4. 以目标驱动执行

先定义成功标准，再循环验证直到达成。

把任务改写成可验证的目标：

- "增加校验" -> "先为非法输入编写测试，再让测试通过"
- "修复 bug" -> "先写出能稳定复现问题的测试，再让测试通过"
- "重构 X" -> "确保修改前后测试都通过"

面对多步骤任务时，先给出简短计划：

```text
1. [步骤] -> 验证：[检查方式]
2. [步骤] -> 验证：[检查方式]
3. [步骤] -> 验证：[检查方式]
```

---

